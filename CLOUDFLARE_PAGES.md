# Cloudflare Pages Deploy

- Root directory: `.`
- Build command: `(none)`
- Output directory: `site`

Notes:

- This deploy is a public landing page for the native app and backend contract.
- The app runtime remains iOS-native and the backend remains a separate service.
